<h5>
    <body>
    <script type="text/javascript">
        (function () {
            var blinks = document.getElementsByTagName('blink');
            var visibility = 'hidden';
            window.setInterval(function () {
                for (var i = blinks.length - 1; i >= 0; i--) {
                    blinks[i].style.visibility = visibility;
                }
                visibility = 'hidden';
            }, 5000);
        })();
    </script>
    {{#if message}}
        <p><div class="form-group"><blink class="alert alert-success alert-dismissible fade show" role="alert" >{{message}}</blink></div></p>
    {{/if}}

    {{#if resultNotEmpty}}
        <table class="table table-hover">
            <thead><tr>
            <td>Наименование товара</td>
            <td> Стоимость товара</td>
            <td>Количество</td>
            </tr>
            </thead>
            {{#each rows}}
                <tbody>
                <tr>
                    <td><a href="/goods/{{good_id}}">{{good_name}}</td>
                    <td>{{price}} руб.</td>
                    <td>{{quantity}} шт.</td>
                    <td>
                        <form action="/cart" method="post">
                            <input name="good_name" value="{{good_name}}" hidden/>
                            <input name="item_id" value="{{item_id}}" hidden/>
                            <button type="submit" class="btn btn-primary btn-dark" >Убрать из корзины</button>

                        </form>
                    </td>
                </tr>
                </tbody>
            {{/each}}
        </table>
        <form action="/order" method="get">
            <p> Общая сумма заказа: {{total}} руб.</p>
            <button type="submit" class="btn btn-primary btn-dark" >Перейти к оформлению заказа.</button>
        </form>
    {{else}}
        <p>В корзине пусто</p>
        <p>Перейдите в <a href="/catalog">каталог</a> чтобы добавить в товар в корзину</p>
    {{/if}}

    </body>
</h5>
<br>
