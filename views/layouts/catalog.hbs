<h5>
    <body>
    {{#if resultNotEmpty}}
    <p></p>
        <table class="table table-hover">

            {{#each rows}}
                <tr>
                    <td><a href="/catalog/{{id}}"><img src="/{{image_url}}" class="rounded" alt="Cinque Terre"></a></td>
                    <td><a href="/catalog/{{id}}">{{name}}</td>
                </tr>
            {{/each}}

        </table>
    {{else}}
        <p>Нет данных</p>
    {{/if}}
    </body>
</h5>
<br>