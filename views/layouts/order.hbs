
<h5><body>
<script type="text/javascript">
    (function () {
        var blinks = document.getElementsByTagName('blink');
        var visibility = 'hidden';
        window.setInterval(function () {
            for (var i = blinks.length - 1; i >= 0; i--) {
                blinks[i].style.visibility = visibility;
            }
            visibility = 'hidden';
        }, 5000);
    })();
</script>
<blink>{{message}} </blink>

<h6>
{{#if resultNotEmpty}}
    <table class="highlight">
        <p>Заказаные товары</p>
        <td>Наименование товара</td>
        <td> Стоимость товара</td>
        <td></td>
        {{#each rows}}
            <tr>
                <td>{{good_name}}</td>
                <td>{{good_price}} руб.</td>
            </tr>
        {{/each}}
    </table>
    <h6>
        <form action="/order" method="post">
        <input  type="text" name="user_id" value="{{userId}}" hidden/>
        <p>Имя получателя:<input type="text" name="user_name" value="{{username}}"/></p>
        <p>Адрес получателя:</p>
            <input required type="text" name="address"  value="{{address}}"/>
        <p> Общая сумма заказа: {{total}}</p>
            <input type="number" name="price" value="{{total}}" руб. hidden/>

            <a class="waves-effect waves-light btn-small">
            <input type="submit" value="Оформить заказ"/>
        </a>
    </form>
    </h6>

{{else}}
    <p>Вы еще не добавили в свою корнзину ни одного товара!</p>
{{/if}}
</h6>

</body></h5>
<br>
